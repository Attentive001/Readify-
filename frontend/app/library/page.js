"use client";
import { useState } from "react";
import { mockBooks } from "../../lib/mockData";
import BookCard from "../../components/BookCard";

export default function LibraryPage() { const [saved, setSaved] = useState(mockBooks.slice(0, 4)); return <main className="max-w-7xl mx-auto px-5 lg:px-8 py-12"><p className="text-xs uppercase tracking-[.2em] text-gold font-bold">Your reading space</p><h1 className="font-display text-4xl sm:text-5xl font-bold mt-2">My Library</h1><p className="mt-3 text-ink/60">Keep your saved books and continue where you left off.</p><section className="mt-10 grid lg:grid-cols-[1fr_320px] gap-10"><div><div className="flex gap-2 border-b border-ink/10 pb-3"><span className="rounded-full bg-ink text-parchment px-4 py-2 text-xs font-bold">Saved books</span><span className="rounded-full px-4 py-2 text-xs font-semibold text-ink/55">Continue reading</span><span className="rounded-full px-4 py-2 text-xs font-semibold text-ink/55">History</span></div><div className="grid grid-cols-2 sm:grid-cols-4 gap-5 mt-7">{saved.map(b => <BookCard key={b.id} book={b}/>)}</div></div><aside className="rounded-3xl bg-ink text-parchment p-7 h-fit"><p className="text-xs uppercase tracking-[.2em] text-amber-300 font-bold">Reading progress</p><h2 className="font-display text-2xl font-bold mt-3">Keep going</h2><p className="text-sm text-parchment/60 mt-2">Your reading progress and bookmarks will appear here when you sign in.</p><div className="mt-6 h-2 rounded-full bg-white/10 overflow-hidden"><div className="h-full w-[67%] bg-amber-300 rounded-full"/></div><div className="mt-2 flex justify-between text-xs text-parchment/55"><span>Atomic Habits</span><span>67%</span></div><a href="/reader/1" className="block text-center mt-6 rounded-full bg-parchment text-ink px-5 py-3 text-sm font-bold">Continue reading</a></aside></section></main>; }
